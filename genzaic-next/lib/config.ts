// Shared app-wide constants.
// Add new variables here grouped by section so they stay easy to find.

// ─── Pricing ──────────────────────────────────────────────────────────────────
export const PLATFORM_FEE_PERCENT = 0.05 // 5%
export const GST_RATE = 0.18 // 18% GST on product price
export const EXAMPLE_PRODUCT_PRICE = 1000 // ₹ used in fee-mode UI examples
